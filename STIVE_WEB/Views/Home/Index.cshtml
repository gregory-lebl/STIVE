@{
    ViewData["Title"] = "Home Page";
    @model List<STIVE_WEB.Models.Article>;
}

<partial name="/Views/Shared/_Header.cshtml" />

@*<div class="text-center">
    @foreach (var item in Model)
    {
        <h1>Nom : @item.Name</h1>
    }
</div>*@
<div class="d-flex align-items-center justify-content-around" style="height: calc(100vh - 180px);" id="landing">
    <div>
        <h1>NEGOSUD, négociant en vins</h1>
        <p>
            Situé en Gascogne dans les Landes, NEGOSUD est un négociant en vin <br />
            soucieux d’apporter à ses clients les meilleurs vins aux meilleurs prix.
        </p>
        <a href="#" class="btn" style="background-color: #7b0405; color: white;">
            Notre sélection de vins
        </a>
    </div>
    <div>
        <img src="~/img/img-landing-page.png" alt="Illustration de vins"/>
    </div>
</div>
